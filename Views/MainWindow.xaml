<Window x:Class="Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="350" Width="525">
    <DockPanel>
        <!-- Navbar -->
        <Grid Background="#2196F3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="C/F" Grid.Row="2" Margin="10" Foreground="White" ></TextBlock>
        </Grid>

        <!-- Content Area -->
        <Grid DockPanel.Dock="Top">
            <StackPanel Margin="10">
                <Label Content="Name:"/>
                <TextBox x:Name="txtName" Margin="0,5"/>

                <Label Content="Email:"/>
                <TextBox x:Name="txtEmail" Margin="0,5"/>

                <Button Content="Submit" Click="Submit"  Margin="0,10"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>